export function randomUUID() { return globalThis.dshServices.crypto.randomUUID(); } export function createHash() { const _d = []; return { update(d) { _d.push(String(d)); return this; }, digest() { const h = globalThis.dshServices.crypto.sha256(_d.join('')); return h; } }; } export function randomBytes(n) { const u = new Uint8Array(n || 16); for (let i = 0; i < u.length; i++) u[i] = Math.floor(Math.random() * 256); return u; }