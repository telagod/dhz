export function fileURLToPath(u) { const s = String(u && u.href !== undefined ? u.href : u); return s.startsWith('file://') ? s.slice(7) : s; } export function pathToFileURL(p) { return { href: 'file://' + p, toString() { return 'file://' + p; } }; } export const URL = globalThis.URL; export const URLSearchParams = globalThis.URLSearchParams;